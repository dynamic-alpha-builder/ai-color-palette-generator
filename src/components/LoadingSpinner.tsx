'use client'

export default function LoadingSpinner() {
  return (
    <span
      className="spinner inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full"
      role="status"
      aria-label="Loading"
    />
  )
}
